# Log di avanzamento — esecuzione PL-01 → PL-33

> Una riga per pacchetto. Stato: fatto / in corso / bloccato / saltato.
> Riferimento tecnico: `PIANO_IMPLEMENTAZIONE_CONSOLIDATO.md` (aggiornato in corso d'opera per la
> fusione UX fase 5/6, vedi nota sotto).
> Audit di partenza: `STATO_IMPLEMENTAZIONE_QUIZZETTONE.md`.

## Nota di ripresa

Un turno precedente aveva già iniziato PL-01, ma le modifiche non erano state committate prima che il
container si resettasse: il lavoro è andato perso. Questa esecuzione riparte da zero da PL-01 (questo
file non esisteva più sul filesystem all'avvio di questo turno).

## Verifica preliminare (Passo 0)

- **FT-01** (ambiente di prova separato): nel codice/git log non esiste alcun dialog di reset a due
  passaggi né una checklist di backup collegata ad `adminResetGame` (verificato con
  `git log --oneline --all` e grep su "backup", "checklist", "due passaggi", "two-step" — nessun
  risultato in entrambi i turni in cui è stato verificato). Punto di decisione **aperto**: verrà
  richiesta conferma esplicita immediatamente prima del pacchetto PL-05.
- **FT-02**: nessuna azione in questa sessione. Copre **PL-04**, saltato.
- **FT-09** (PL-09): richiede conferma esplicita prima di iniziarlo, in Fase 2.

## Nota fusione UX fase 5/6

Da `AGGIORNAMENTO_PIANO_UX_fase5.md` (allegato dall'utente): a fine PL-24, PL-25/PL-27/PL-28/PL-30/
PL-32 del piano originale vengono sostituiti da **PL-25-UX**, **PL-27-UX**, **PL-30-UX**.
`PIANO_IMPLEMENTAZIONE_CONSOLIDATO.md` verrà aggiornato di conseguenza prima di eseguirli.

## Aggiornamento Passo 0 — FT-01 confermato

L'utente ha confermato esplicitamente la proposta di default: dialog di reset a due passaggi +
checklist di backup manuale, non il secondo progetto Firebase del piano originale.
`PIANO_IMPLEMENTAZIONE_CONSOLIDATO.md` è stato aggiornato di conseguenza (sezione PL-05 riscritta,
commit `2b55d31`) prima di eseguire il pacchetto.

## Gate iniziale completato

Tutti gli 8 pacchetti del gate iniziale (Fase 0-1) sono conclusi: PL-04 (saltato), PL-01, PL-02, PL-03,
PL-08, PL-05, PL-06, PL-07.

## Aggiornamento Passo 0 — FT-09/PL-09 confermato

L'utente ha confermato esplicitamente di procedere con PL-09 come descritto nel piano originale
(rami separati sotto `sessions/current/...`, `sessionId` fisso, niente multi-sessione vera).

## Aggiornamento — chiarimento su un riferimento a un file inesistente

Un turno ha citato un presunto file `AGGIORNAMENTO_PIANO_PL09_flat.md` e un'istruzione precedente di
eliminare PL-09: nessuno dei due esiste in questa sessione (verificato: nessun file simile nel repo,
nessuna istruzione simile nella cronologia). Segnalato e proseguito senza alcuna azione su PL-09.

## Aggiornamento — PL-13 come "solo plumbing"

Prima di eseguire PL-13, è stato chiesto esplicitamente all'utente come trattare il fatto che i
meccanismi che rendono "Dinamico" davvero diverso da "Classico" (bonus ordine, penalità a fascia, bonus
rimonta) sono pacchetti separati e successivi (PL-14/PL-15): unire tutto in un pacchetto solo, oppure
introdurre in PL-13 solo l'interruttore di profilo e le tabelle di default riservate, lasciando il
calcolo dei punti invariato finché PL-14/15 non lo cambiano davvero. L'utente ha scelto la seconda opzione
(consigliata): pacchetti più piccoli e verificabili uno alla volta, coerente col resto della sessione.

## Aggiornamento — PL-11 allargato con uno scope ridotto da PROPOSTA_SCHERMO_CONDIVISO_completa.md

Prima di eseguire PL-11, l'utente ha chiesto di farvi confluire un sottoinsieme ridotto (9 punti,
esclusioni esplicite) di un documento di riferimento più ampio (`PROPOSTA_SCHERMO_CONDIVISO_completa.md`,
progetto completo di modalità "schermo condiviso", non implementato per intero) per gettare le
fondamenta di una futura modalità Display, cogliendo l'occasione che PL-11 tocca comunque il ciclo di
vita della domanda. `PIANO_IMPLEMENTAZIONE_CONSOLIDATO.md` è stato aggiornato di conseguenza (sezione
PL-11 riscritta con nota di revisione, forward-note aggiunta a PL-18 per i due campi riservati
`presentation.mode`/`sharedScreenRequirement`; commit `a6b4bcd`) prima di eseguire il pacchetto, stesso
schema già usato per PL-05.

## Log pacchetti

| ID | Titolo | Stato | Commit | Note/deviazioni |
|---|---|---|---|---|
| PL-04 | Verificare/versionare regole Firebase | saltato | — | Copre FT-02: "nessuna azione in questa sessione" per decisione esplicita dell'utente (Passo 0). |
| PL-01 | Chiudere l'XSS residuo | fatto | 185581f | Aggiunto helper `teamNameHtml()` in js/render.js e sostituite tutte le interpolazioni dirette di nome squadra non escapate (rankRows, renderTeam, lobby Admin, tabelle Admin, blocchi spareggio/finale). Test mirati (phase1-lobby, smoke) + nuovo tests/e2e/phase1-xss.spec.js tutti verdi. |
| PL-02 | Loading, timeout, errore, retry all'avvio | fatto | a97a80b | Aggiunti `bootStatus`/`retryBoot()` in js/state.js (timeout 8s su `startListening`) e `renderBootScreen()` in js/render.js, richiamata da `render()` quando `!state` invece di lasciare la pagina bianca. Nuovo test tests/e2e/phase1-boot-recovery.spec.js (simula rete offline via `context.setOffline`, verifica loading→timeout→retry funzionante). Non toccato quizzettone.html: tutta la logica sta in state.js/render.js, non serve nulla nello script di boot. Suite completa non rieseguita (interrotta in background per evitare conflitti sull'emulatore condiviso); rieseguiti invece i test mirati più ampi (lobby, smoke, timer, xss) oltre a quello nuovo — tutti verdi. |
| PL-03 | Gestire gli esiti reali delle operazioni Firebase | fatto | b9c6048 | Aggiunto `showErrorBanner()` in js/render.js. `withUndo` ora ritorna esito e mostra errore+retry se `safeSet` fallisce (copre indirettamente `adminAdjustPoints`, che delega tutto a `withUndo`). Controllato esplicitamente il ritorno di `safeSet` in `adminStartGame`, `adminSaveSetup`, `adminResetGame`, `teamJoin`, `teamSubmitAnswer`: su fallimento non si avanza più lo stato locale (es. `teamJoin` non assegna più `teamId`/`teamName` prima di sapere se la scrittura è riuscita). Fuori scope (non toccato, come da elenco file del pacchetto): le `safeDelete` (silenziose per disegno) e i `db.ref(...).update()` diretti altrove nel codice. Test nuovo tests/e2e/phase1-write-errors.spec.js: simula un fallimento reale di `.set()` via monkey-patch di `db.ref` (non tramite regole Firebase, per non toccare FT-02) su join/risposta/reset, verifica banner+retry funzionante per tutti e tre. Suite mirata (lobby, smoke, xss, undo, boot-recovery) tutta verde. |
| PL-08 | Validazione e limiti sui file media | fatto | c5ecc5a | `uploadAudioFile` (js/firebase-init.js) ora rifiuta prima dell'upload i file non audio o oltre 8MB, con un `Error` dal messaggio chiaro. Non serve toccare js/render.js (a differenza di quanto ipotizzato dal piano): i due punti di chiamata (form domande, form effetti) avevano già un try/catch che mostra `e.message` nello stesso punto dove appare "Caricamento audio...", quindi ereditano il messaggio d'errore gratis. Nuovo test tests/e2e/phase1-media-validation.spec.js (tipo sbagliato + file sovradimensionato, su entrambi i punti di upload). Suite di regressione mirata (smoke, testmode-stats) verde. |
| PL-05 | Reset a due passaggi con checklist di backup (versione rivista, ex "ambiente di prova separato") | fatto | 8380415 | Nuova `showResetChecklistModal()` in js/render.js: Step 1 checklist a 3 caselle (nessuna preselezionata) con "Continua" disabilitato finché non sono tutte spuntate; Step 2 conferma finale separata che è l'unico punto a chiamare `adminResetGame`. `btnReset` non usa più `showConfirmModal` generica. Aggiornati due test esistenti che assumevano il vecchio flusso a un solo step (`phase1-undo.spec.js`, `phase1-write-errors.spec.js`) più un nuovo test dedicato che verifica che il reset vero e proprio non scatti finché entrambi gli step non sono completati. Suite mirata (undo, write-errors, smoke) verde. |
| PL-06 | Invio atomico delle risposte | fatto | 60e2a76 | `teamSubmitAnswer`/`adminSubmitTestAnswer` ora usano `db.ref(...).child(key).transaction(...)` sul singolo nodo `answers:<id>/<key>` invece di read-then-write sull'intero oggetto `answers:<id>`; "già risposto" (`current` esiste) non è più trattato come errore. Nuovo flag locale `pendingSubmitKey` (js/state.js) blocca subito tutte le opzioni al click e sopravvive ai re-render del tick da 250ms, con nuovo banner "Invio in corso...", riabilitato solo se la transazione fallisce davvero. Aggiornato `failNextWriteTo` in `phase1-write-errors.spec.js` per intercettare anche `.transaction()` attraverso `.child()` (non solo `.set()`), perché il vecchio monkey-patch non l'avrebbe più intercettata. Nuovo file `phase1-atomic-submit.spec.js`: due chiamate concorrenti a `teamSubmitAnswer` per la stessa squadra/domanda registrano sempre una sola risposta senza errore per il "perdente"; i pulsanti restano disabilitati per l'intera finestra di invio. Suite mirata (write-errors, timer, speed-bonus, testmode-stats, smoke) verde. |
| PL-07 | Join squadra atomico e univoco per nome | fatto | d1c0bb6 | Nuovo indice `teamNames:<nome-normalizzato>` (js/actions.js, helper `teamNameKey`) scritto con `transaction()`: chi vince crea la squadra, chi perde recupera l'id già creato dal vincitore (con un piccolo retry/attesa per la finestra non atomica tra "vinta la transazione sul nome" e "scritta teaminfo:"). Rimossa `findTeamByName` (scansione lineare, sostituita dall'indice). Nuova `adminRenameTeam` (aggiorna sia `teaminfo:` sia l'indice, rifiuta se il nuovo nome è già preso) + pulsante "✎" nella lobby Admin con un piccolo modale dedicato (`showRenameTeamModal`, niente `window.prompt` nativo). `adminRemoveTeam` ora libera anche l'indice del nome rimosso. **Limite noto, non risolto in questo pacchetto:** "Annulla ultima azione" dopo una rimozione squadra ripristina `teaminfo:` ma non ricrea l'indice del nome (l'undo generico salva/ripristina un solo path, non è a conoscenza degli effetti collaterali sull'indice) — un caso limite raro, non il percorso principale che il criterio del pacchetto chiede di coprire. Nuovo file `phase1-team-identity.spec.js`: due join quasi simultanei con lo stesso nome producono una sola squadra (due dispositivi separati, non due tab della stessa squadra come in PL-06); rinomina funzionante; rinomina rifiutata se il nome è già preso. Suite mirata (lobby, undo, write-errors, smoke) verde. |
| PL-09 | Modello dati di sessione strutturato | fatto | 296c3a6 | Nuovi helper di percorso in `js/firebase-init.js` (`sessionPath`, `statePath`, `teamPath`, `answersPath`, `scoreLedgerPath`, `teamNamePath`, `presencePath`, `questionBankPath`, `soundEffectPath`; `SESSION_ID='current'` fisso). `safeList` ridefinita da "scan del nodo piatto" a "elenca figli di un percorso". `js/state.js`: nuova variabile locale `gameQuestions` (era `state.gameQuestions`) popolata dal ramo separato `sessions/current/questionInstances`; `startListening()` legge rami annidati invece di smistare per prefisso stringa; nuova `migrateFlatStateToSessions()` one-shot (solo Admin) che sposta tutto il vecchio nodo piatto in un solo `update()` atomico. `js/actions.js`: riscritto interamente (sostituzione dei ~50 punti che usavano `'state'`/`'teaminfo:'+id`/ecc. con gli helper); `adminStartGame`/`adminContinueToFinal`/`adminStartTiebreakQuestion` scrivono `state` e `questionInstances` in un solo `update()` atomico multi-path; `adminResetGame` riscritta per pulire tutti i rami di sessione. Scelta di scope deliberata (documentata nel piano, sezione PL-09): `state` resta un nodo combinato config+runtime (non split in due), per contenere il rischio su un pacchetto già XL. **Due bug reali trovati e corretti durante la verifica, non nel piano originale:** (1) race tra la migrazione e `ensureJoinCode`/`ensureSeedQuestions` sullo stesso primo snapshot — `migrateFlatStateToSessions` ora ritorna un booleano e le tre `ensure*` vengono saltate sul giro in cui la migrazione ha davvero spostato qualcosa, per non sovrascrivere il dato appena migrato con dei default; (2) il criterio "c'è qualcosa da migrare" guardava solo `all.state`, ma Firebase pota silenziosamente un valore `{}`, quindi un database con solo domande legacy (`question:...`) e nessuno `state` vero non veniva mai migrato — ora il criterio controlla anche gli altri prefissi legacy (`hasLegacyFlatData`). Nuovo test dedicato `phase2-session-migration.spec.js` (seed diretto via REST di dati in forma piatta, verifica che la UI li legga correttamente e che le vecchie chiavi spariscano). Aggiornati anche alcuni test preesistenti che referenziavano percorsi piatti direttamente (`phase1-write-errors.spec.js`, `phase1-timer.spec.js`, `phase2-finals.spec.js` — inclusa l'aggiunta di un caricamento Admin "bootstrap" in un test che prima apriva solo pagine Team, scenario innaturale rispetto all'uso reale). **Suite e2e completa eseguita due volte fino al verde** (41/41, un singolo flake di chiusura contesto non riproducibile in isolamento). |
| PL-10 | Snapshot di configurazione per domanda + versione | fatto | ef3c1a1 | Nuovo campo `scoringSnapshot` scritto su ogni istanza di domanda al momento dell'apertura (`computeEffectiveScoringForOpen`/`withScoringSnapshotApplied` in js/state.js), tramite un nuovo helper condiviso `openQuestionWithSnapshot` (js/actions.js) usato da tutti i punti che aprono una domanda: `adminStartGame`, `adminNextQuestion` (entrambi i rami), `adminContinueFromCheckpoint` (entrambi i rami), `adminContinueToFinal`, `adminStartTiebreakQuestion`. `scoringFor()` ora legge lo snapshot della domanda come "punteggio di default" (l'override esplicito per singola domanda resta comunque sopra, intenzionalmente: è lo strumento con cui l'admin corregge un punteggio dopo il fatto). Aggiunto `config.version` (parte da 0, incrementato a ogni `adminSaveSetup` riuscito). Nuovo file `phase2-scoring-snapshot.spec.js`: verifica end-to-end che un cambio di `state.config.scoring` a partita in corso (simulato via scrittura diretta Firebase, perché non esiste ancora un percorso UI per farlo — arriverà con PL-25) non alteri il punteggio di domande già aperte, e che una domanda aperta DOPO il cambio riceva invece la nuova regola; verifica separata che `config.version` si incrementi a ogni salvataggio. Suite di regressione mirata (rounds, speed-bonus, finals, standings-reveal, testmode-stats, regia-checklist, smoke — 14 test) verde. |
| PL-11 | Tempo autorevole condiviso + fasi presentazione/sblocco/timer separate (scope allargato, vedi nota sopra) | fatto | c7baa5a | **Parte originale (tempo autorevole):** nuovo `serverTimeOffset`/`serverNow()` (js/state.js), alimentato da un listener su `.info/serverTimeOffset` in `startListening()`; sostituito `Date.now()` con `serverNow()` in tutti i punti critici per il timer in js/actions.js (`openQuestionTimer`, `adminStartTimerManually`, `adminResumeTimer`, `adminAdjustTimer`, `closeAnswersTransactional`, timestamp `ts` di `teamSubmitAnswer`/`adminSubmitTestAnswer`) e in `timeRemaining()` (che vive in js/render.js, non js/state.js come indicava la vecchia voce di piano — corretto anche lì). Nessun'altra chiamata `Date.now()` toccata (joinedAt, createdAt, audioCue, history, gameId restano invariati). **Parte aggiunta (9 punti ridotti da PROPOSTA_SCHERMO_CONDIVISO_completa.md):** nuovo `config.displayMode` (`team_devices`/`shared_screen`/`hybrid`, selezionabile in Sala pre-partita, default `team_devices`); tre timestamp per istanza di domanda — `presentedAt`/`inputUnlockedAt`/`timerStartedAt` — scritti tramite un nuovo helper generico `applyQuestionInstanceFields` (fattorizzato da `withScoringSnapshotApplied`, che ora lo richiama, comportamento esterno invariato) su tutti i punti che aprono una domanda; `resolvePresentationMode()`/`answerUnlockPolicyFor()` risolvono una sola volta, in `adminStartGame`, la policy di sblocco (`immediate` per team_devices, `manual` per shared_screen/hybrid) scritta in `state.presentation.resolvedMode`. Scelta di implementazione a basso rischio: la policy `manual` riusa esattamente il meccanismo `timer.status==='idle'` già esistente per `config.timerStartMode==='manual'` (forzato anche quando `timerStartMode` è `'auto'`), quindi "Apri risposte" è lo stesso bottone/azione di sempre (`adminStartTimerManually`, solo etichetta diversa), non una nuova azione parallela — sblocco input e avvio timer restano quindi sempre un solo evento atomico, come richiesto. Nuovo fallback per-domanda `presentation.fallbackActive` con azione `adminActivateTeamFallback` e bottone "Mostra domanda sui telefoni" (mostra la domanda sul telefono senza sbloccare da sola l'input). Vista Team (`renderTeamQuestion`, js/render.js): con sblocco manuale e input ancora bloccato mostra solo "📺 Guarda lo schermo condiviso" al posto di domanda/opzioni, a meno che il fallback non sia attivo. Nuovo `armDisplayPresence()`/`disarmDisplayPresence()` (js/state.js): heartbeat `connected`+`lastSeenAt` ogni 2.5s su `presencePath('display', connId)` con `onDisconnect().remove()`, stesso pattern della presenza squadra, agganciato al ruolo `'display'` già esistente in `restoreFromUrl`; nessuna UI Display costruita in questo pacchetto (i dati sono verificati solo dai test, leggendo il ramo Firebase direttamente). **Esplicitamente non implementato** (fuori scope anche nel documento di riferimento completo): `instanceId`/`activityId`/`revision` sulle risposte, registro `ACTIVITY_TYPES`, `displayPresence.capabilities`, switch lettere/testo, `spectatorContentMode` configurabile. Nuovo file `phase2-shared-screen-presentation.spec.js` (3 test: sblocco manuale bloccato poi aperto con timestamp coerenti, fallback per-domanda, heartbeat Display connesso/rimosso alla disconnessione). **Suite e2e completa eseguita fino al verde** (46/46): un primo passaggio ha segnalato 3 fallimenti (`phase1-atomic-submit`, `phase1-team-identity`, `smoke`) tutti per timeout di navigazione dopo 20 minuti di esecuzione continua a worker singolo; rieseguiti in isolamento subito dopo, tutti verdi — flakiness da infrastruttura (contesa emulatore/browser su una suite lunga), non regressione introdotta da questo pacchetto. |
| PL-12 | Sala pre-partita: riepilogo e preset | fatto | 7838223 | Nuovi 3 pulsanti preset built-in in `renderSalaPrePartita` (`BUILTIN_CONFIG_PRESETS`, js/render.js) — Serata classica, Partita rapida, Tutti contro tutti — con i valori dichiarati da `Piano_modifiche_Quizzettone.md` §4.1, applicati al form tramite `applyPresetValuesToForm()` senza un render() completo (stesso principio già usato da `btnToggleAdvancedSetup`, per non perdere modifiche non ancora salvate). **Scelte di interpretazione, documentate qui perché il piano consolidato non le specifica in dettaglio:** "Personalizzata" non ha un bottone dedicato — è già lo stato di default del form, liberamente modificabile, coerente con "nessun nuovo motore, solo default diversi"; "Tutti contro tutti" ("nessuna squadra eliminata nell'ultima manche") non richiede nuova logica di gioco — imposta `finalistCount:999`, che fa cadere `adminRevealFinalists` nel ramo già esistente `ranked.length<=finalistCount` (tutti finalisti, nessuno eliminato). Nuovo nodo globale `presets/` (helper `presetPath()` in js/firebase-init.js; persiste tra le partite come `questionBank`/`soundEffects`/`mazzi`, non toccato da `adminResetGame`): `adminSaveConfigPreset(name, config)`/`adminDeleteConfigPreset(id)` in js/actions.js; nuovo bottone "💾 Salva impostazioni attuali come preset" (modale `showSavePresetModal`) e lista preset salvati con Carica/Elimina. Riepilogo pre-start (item 12): `btnStart` ora apre `showStartSummaryModal(state.config, adminStartGame)` — un riepilogo testuale di sola lettura (manche, finale, punteggio, timer, modalità di presentazione, punti in ingresso finale) con conferma esplicita prima di chiamare davvero `adminStartGame`; annullare riporta al form senza avviare nulla. Fattorizzato `collectSetupPatchFromForm(includeAdvancedAlways)` fuori dall'handler di `btnSaveSetup` (comportamento invariato) per essere riusato anche dal salvataggio preset (che legge sempre i campi avanzati, anche a sezione chiusa, essendo un'istantanea a sé stante). **Bug reale trovato e corretto durante la verifica:** `applyPresetValuesToForm` assegnava l'array `questionsPerRound` (stessa forma del patch, es. `[7,7,7]`) direttamente a `input.value`, producendo la stringa `"7,7,7"` invece di `"7"` — corretto leggendo `values.questionsPerRound[0]` quando è un array (i preset built-in dichiarano invece un numero semplice). **Cambio di comportamento che ha rotto 21 chiamate `#btnStart` in 12 file di test preesistenti** (ogni click su "Avvia Manche 1" ora apre il modale di riepilogo invece di avviare subito la partita): tutte aggiornate per cliccare anche `#startSummaryConfirm` subito dopo (il test dedicato al riepilogo in `phase2-setup-presets.spec.js` gestisce il flusso manualmente, incluso un caso di annullamento). Nuovo file `phase2-setup-presets.spec.js` (4 test: preset "Partita rapida" precompila 1/10/2/5, preset "Tutti contro tutti" apre la sezione avanzata e imposta `finalistCount:999`/`scoreCarryover:'keep'`, un preset salvato sopravvive a un ricaricamento della pagina ed è ricaricabile, il riepilogo compare prima dello start e annullare non avvia nulla). **Suite e2e completa eseguita fino al verde** (50/50): un primo passaggio ha segnalato 4 fallimenti (`phase1-rounds`, `phase1-setup-screen`, `phase1-team-identity`, `smoke`), tutti timeout di navigazione/click dopo 22 minuti di esecuzione continua a worker singolo; rieseguiti in isolamento subito dopo, tutti verdi — stessa flakiness da infrastruttura già osservata a fine PL-11, non regressione. |
| PL-13 | Motore di scoring a profili (solo plumbing, vedi nota sopra) | fatto | 33976b8 | Nuovo `config.scoringProfile` (`'classico'` default \| `'dinamico'` \| `'personalizzato'`) e `config.dynamicScoring` (tabelle di default da `Piano_modifiche_Quizzettone.md` §4.2: `orderBonusPercents:[25,20,15,10,5]`, `wrongPenaltyRate:0.20`, `penaltyBands` con le 5 fasce/moltiplicatori/bonus rimonta consigliati) in `defaultState()`, fusi in `withDefaults()` con lo stesso pattern shallow-merge già usato per `scoring`/`tiebreakRule`/`lateJoin`. Selettore in Sala pre-partita (sezione avanzata), con una nota esplicita sotto il menu che avvisa l'admin che "Dinamico"/"Personalizzato" non cambiano ancora il calcolo; il profilo scelto compare anche nel riepilogo pre-start di PL-12. **Per decisione esplicita dell'utente (vedi nota sopra), `pointsForAnswer`/`scoringFor` restano invariati per qualunque profilo**: nessuna lettura di `scoringProfile`/`dynamicScoring` a runtime in questo pacchetto — lo faranno davvero PL-14 (bonus ordine) e PL-15 (penalità adattiva + rimonta). Nuovo `tests/unit/scoring.spec.js`, prima suite di test non-e2e del repo: eseguita con il test runner nativo di Node (`node --test`, script npm `test:unit`), non Playwright — scelta deliberata per non introdurre una seconda configurazione `testDir`/progetto Playwright solo per test che non toccano affatto un browser o l'emulatore. Reso possibile senza toccare l'architettura a `<script>` globali del resto del codice: in fondo a `js/state.js` un blocco `if(typeof module !== 'undefined' && module.exports){ module.exports = {...} }` (mai eseguito nel browser, dove `module` non esiste) espone le funzioni pure di scoring più due setter di comodità (`__setTestState`/`__setTestGameQuestions`/`__setTestScoringDefaults`) per preparare lo stato locale nei test senza passare da Firebase. 5 test unitari: default e merge di `scoringProfile`/`dynamicScoring` (incluso un merge parziale, stesso pattern di retrocompatibilità del resto del file), e soprattutto `pointsForAnswer` che produce **esattamente lo stesso risultato** per i tre profili — un test di non-regressione che blocca esplicitamente una futura modifica che cambiasse il punteggio prima che PL-14/15 siano stati eseguiti. Nuovo `tests/e2e/phase2-scoring-profile.spec.js` (2 test: il profilo scelto sopravvive a un salvataggio+ricaricamento; selezionare "Dinamico" non altera il punteggio di una risposta corretta in una partita reale) — il primo test ha richiesto di alzare `test.setTimeout` da 30s a 60s (stesso valore già usato altrove nel repo per test che includono un reload di pagina), il valore iniziale era semplicemente troppo stretto per PIN+form+save+reload, non un bug applicativo. **Suite e2e completa e suite unitaria eseguite entrambe fino al verde** (52/52 e2e, 5/5 unit). |
